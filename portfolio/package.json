{
  "name": "project-sovereign-portfolio",
  "private": true,
  "description": "Static showcase for Project Sovereign — dev tooling (audit before Pages deploy)",
  "type": "commonjs",
  "scripts": {
    "serve": "node scripts/serve.mjs",
    "audit": "node scripts/audit.js",
    "check:copy": "node scripts/check-copy.mjs",
    "check:copy:selftest": "node scripts/check-copy.mjs --self-test"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
